# Operator secrets — never commit. See spec FR-016.
subscriptions.csv
own-proxies.yaml
tokens.json
custom-rules
custom-rules/**

# Operator-private deploy notes — environment-specific (cluster contexts,
# namespaces, Argo app names). Not appropriate to publish.
*

# But DO commit this gitignore and the README
!.gitignore
!README.md
